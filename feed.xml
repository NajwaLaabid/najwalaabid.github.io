<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://najwalaabid.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://najwalaabid.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-07T13:47:03+00:00</updated><id>https://najwalaabid.github.io/feed.xml</id><title type="html">blank</title><subtitle>I write about my work and other topics I like in ML research. </subtitle><entry><title type="html">How well can an LLM-Agent select assays for transfer learning?</title><link href="https://najwalaabid.github.io/blog/2026/where-llm-agents-help-drug-discovery/" rel="alternate" type="text/html" title="How well can an LLM-Agent select assays for transfer learning?"/><published>2026-06-24T00:00:00+00:00</published><updated>2026-06-24T00:00:00+00:00</updated><id>https://najwalaabid.github.io/blog/2026/where-llm-agents-help-drug-discovery</id><content type="html" xml:base="https://najwalaabid.github.io/blog/2026/where-llm-agents-help-drug-discovery/"><![CDATA[<p>Modern drug discovery leans hard on prediction: rather than measure every property of every candidate in the lab, you train models to guess them <em>in silico</em> and only run the measurements you have to. When a property has too few labels to learn from, the standard trick is to borrow what a model already picked up from a related, better-measured property, also known as <em>transfer learning</em>. The catch is deciding which property to borrow from, and that is exactly the kind of judgement call you might hope an AI agent could make. Here’s where it can, and where a twenty-line heuristic does the job just as well.</p> <h2 id="why-evaluating-molecules-is-slow-and-costly">Why evaluating molecules is slow and costly</h2> <p>Before a molecule can become a drug, you need to know how it behaves in the body: how much of it is <strong>A</strong>bsorbed, how it <strong>D</strong>istributes, how fast it is <strong>M</strong>etabolised and <strong>E</strong>xcreted, and how <strong>T</strong>oxic it is. These five properties are known collectively as ADMET <d-cite key="waterbeemd2003admet"></d-cite>. Each one is a separate lab measurement, also known as an <em>assay</em><d-footnote>This is also what we will call a set of measured compounds for one endpoint, i.e. a column in a dataset of molecular properties.</d-footnote>, where multiple candidate compounds are tested together whenever possible.</p> <p>Some assays are cheap, like an aqueous-solubility readout<d-footnote>The measurement is done by diluting a compound into aqueous buffer and reading off how much stays dissolved. Hundreds of compounds can be tested at once, with no cells or animals to keep alive.</d-footnote>, while a half-life toxicity endpoint (LD50) requires dosing animals with one compound at a time and drawing timed blood samples <d-cite key="kerns2016druglike"></d-cite>. For endpoints like the latter, requiring days of wet-lab work and real money per compound, assays often start with only 10–50 compounds.</p> <table> <thead> <tr> <th>ADMET stage</th> <th>The question it answers</th> <th>Common endpoints (<strong>bold</strong> = OpenADMET · <em>italic</em> = TDC)</th> </tr> </thead> <tbody> <tr> <td><strong>A</strong>bsorption</td> <td>Does it get into the bloodstream?</td> <td><strong><em>solubility (KSOL)</em></strong>, <strong><em>permeability (Caco-2)</em></strong>, <strong>efflux (Caco-2 ratio)</strong>, <strong><em>lipophilicity (LogD)</em></strong>, <em>intestinal absorption (HIA)</em>, <em>P-gp inhibition</em>, <em>oral bioavailability</em></td> </tr> <tr> <td><strong>D</strong>istribution</td> <td>Where in the body does it go?</td> <td><strong><em>protein binding (MPPB / MBPB / MGMB; PPBR)</em></strong>, <em>volume of distribution (VDss)</em>, <em>blood–brain barrier (BBB)</em></td> </tr> <tr> <td><strong>M</strong>etabolism</td> <td>How fast is it broken down?</td> <td><strong><em>microsomal clearance (HLM, MLM CLint)</em></strong>, <em>hepatocyte clearance</em>, <em>CYP 2C9 / 2D6 / 3A4 inhibition &amp; substrate</em></td> </tr> <tr> <td><strong>E</strong>xcretion</td> <td>How is it cleared from the body?</td> <td><em>half-life</em>, renal clearance</td> </tr> <tr> <td><strong>T</strong>oxicity</td> <td>Could it cause harm?</td> <td><em>LD50</em>, <em>hERG (cardiac)</em>, <em>Ames (mutagenicity)</em>, <em>DILI (liver injury)</em></td> </tr> </tbody> </table> <p class="caption"><strong>Table 1.</strong> Common ADMET endpoints by stage; <strong>bold</strong> = OpenADMET, <em>italic</em> = TDC.</p> <p>A reasonable move is to train a model to predict the assay from a molecule’s structure, then screen the rest in silico. But a model needs labels, and the endpoints that matter most are the slow, expensive ones where labels are scarcest. Training from scratch on 10–50 measurements gets us nothing. To show this, we train the same model on n=5 observations from 9 different endpoints in turn, and record a mean test Spearman<d-footnote>Spearman's rank correlation (ρ) scores how well predictions <em>rank</em> compounds rather than how close they are in absolute value, which is what matters when the task is to pick the best few to take forward.</d-footnote> of <strong>0.06</strong>, indistinguishable from guessing.</p> <p>The standard fix is transfer learning: instead of training from scratch, pretrain the model on past assays you already have labels for, then fine-tune it on the few new ones <d-cite key="pan2010survey"></d-cite>. The catch is <em>which</em> past assays to pretrain on. Take lipophilicity (LogD) with only five labels, pretraining on one other assay at a time: a mechanistically related source like plasma protein binding lifts Spearman from noise (ρ≈0) to 0.57, while an unrelated one like Caco-2 efflux pushes it <em>below</em> the no-transfer line (negative transfer).</p> <figure class="expdisc-figure"> <div class="expdisc-chart" id="expdisc-fig-negtransfer" style="width:100%; height:460px;"></div> <noscript> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-negative_transfer-480.webp 480w,/assets/img/expdisc-negative_transfer-800.webp 800w,/assets/img/expdisc-negative_transfer-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-negative_transfer.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </noscript> </figure> <p>Which assays help is usually settled by a senior scientist’s intuition, but there are cheap signals you can compute in addition: how strongly a candidate assay correlates with the target (which requires enough measurements), how much the assays’ molecules overlap, or classical transferability scores <d-cite key="nguyen2020leep,you2021logme"></d-cite> like LEEP and LogME, which estimate how well a source will transfer from its model’s features alone, without paying to fine-tune on it first.</p> <p>In this project, we explore how well an LLM-agent chooses the assays to pretrain on compared to traditional baselines. We hold the predictor fixed (a plain MLP where we swap the final layer when training on each different property) so any improvement comes from <em>selection</em>, not a fancier model. Then we ask two things: 1) can an LLM agent choose a better set of source assays than these baselines?, and 2) can it run the whole pipeline (selecting the assays, deciding how many new compounds to measure, and choose which specific compounds to use in the new assay), end to end on its own?</p> <p>The <a href="https://admet-demo-diffalign.2.rahtiapp.fi/">interactive demo</a> and the <a href="https://github.com/NajwaLaabid/assay-transfer-agent">code</a> are online if you’d rather see the agnet at work first; otherwise, read on for how we got there.</p> <h2 id="the-setup">The setup</h2> <p>To test whether an agent can learn ADMET properties better, we use <strong>OpenADMET ExpansionRx</strong> <d-cite key="openadmet2025expansionrx"></d-cite>, a real lead-optimization dataset from the recent OpenADMET blind challenge: 5,326 training and 2,282 test molecules, each measured on the same nine assays. Because every molecule carries all nine readings, the assays are densely co-measured — which, as we’ll see, is exactly what makes a plain correlation such a strong baseline here. To probe the opposite regime we add the <strong>Therapeutics Data Commons</strong> (TDC) ADMET collection <d-cite key="huang2021tdc"></d-cite>: 22 assays drawn from separate studies whose molecules barely overlap, so most source–target pairs share too few compounds to correlate at all. OpenADMET tests selection when the cheap signal is trustworthy; TDC tests it when that signal is gone.</p> <figure class="expdisc-figure"> <div class="expdisc-chart" id="expdisc-fig-coverage" style="width:100%; height:460px;"></div> <noscript> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-data_coverage-480.webp 480w,/assets/img/expdisc-data_coverage-800.webp 800w,/assets/img/expdisc-data_coverage-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-data_coverage.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </noscript> </figure> <p>The predictor is the same simple model in every experiment, with or without the agent: a small neural network<d-footnote>A shared-trunk MLP, 2248&rarr;512&rarr;128. Each molecule enters as a SMILES string, which RDKit turns into a fixed 2248-number vector: a 2048-bit ECFP4 fingerprint (a Morgan fingerprint of radius 2 — a bit-vector recording which substructures the molecule contains) concatenated with ~200 RDKit 2D physicochemical descriptors (molecular weight, LogP, hydrogen-bond counts, and the like). This featurization is deliberately plain; a fancier representation — a graph neural network over the molecular graph, learned embeddings, or richer descriptors — would likely predict better, but we hold it fixed on purpose so any gain traces to source _selection_, not the model.</d-footnote> that turns a molecule’s structure into a predicted assay value. We pretrain it on the chosen source assays, freeze it, and fit a simple linear read-out (a ridge regression) on the few target labels.</p> <p>Every experiment runs the same rotation: one endpoint is the <strong>target</strong>, the rest are candidate <strong>sources</strong>, and we cycle through all nine so each is the target in turn. The target contributes only $n$ labels to the read-out — we sweep $n \in {5, 10, 25, 50}$ to trace the cold-start-to-warm curve — while sources bring their full label sets. Every number is a mean over five random draws of those $n$ labels. Sources are chosen on a held-out split of the target’s own train molecules; the reported Spearman is always on the untouched official test set.</p> <p>For the agent we use CSC’s <a href="https://zenodo.org/records/17305233">Aitta</a> inference service running <strong>Llama-3.3-70B-Instruct</strong>. We pick this open-weights model for its reasoning and its reliable tool use (following instructions and returning well-formed choices). Aitta serves the model on LUMI’s AMD MI250X GPUs behind an OpenAI-compatible REST endpoint, so every step of the agent is a plain chat-completion call from our own machine (no local GPU), and the same code would run against any hosted model by swapping one string.</p> <h2 id="first-the-baselines">First, the baselines</h2> <p>Before handing anything to an agent, it’s worth knowing what can be achieved with existing heuristics. Every method here uses the <em>same</em> fixed predictor (the MLP described earlier); the only difference is which source assays are picked to pretrain on. Five rungs, floor to ceiling: <strong>no transfer</strong> at all, a <strong>random</strong> pair of sources, <strong>all</strong> sources pooled, a <strong>correlation heuristic</strong> that ranks sources by how strongly they track the target and keeps adding until the score stops improving, and a brute-force <strong>oracle</strong> that tries every possible subset and keeps the best.</p> <figure class="expdisc-figure"> <div class="expdisc-chart" id="expdisc-fig-baselines" style="width:100%; height:480px;"></div> <noscript> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-s1_method_lines-480.webp 480w,/assets/img/expdisc-s1_method_lines-800.webp 800w,/assets/img/expdisc-s1_method_lines-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-s1_method_lines.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </noscript> </figure> <p>Two things fall out of this ladder. First, <strong>transfer is the whole game</strong>: going from no-transfer to <em>any</em> pretraining is a 5× jump, far bigger than any gap between selection methods. Second, <strong>pooling everything is not the same as choosing well</strong>: “all sources” sits at 0.46 while the oracle reaches 0.53, and a curated subset beats all-source pooling on 8 of 9 targets. Combining many assays mitigates negative transfer, so the pool never drops below the floor; but the effect is still visible as <em>dilution</em>, which is exactly why the pretraining sources are worth choosing well. The best subset is small: performance peaks at two or three well-chosen sources, then decays steadily as you pile on more.</p> <figure class="expdisc-figure"> <div class="expdisc-chart" id="expdisc-fig-dilution" style="width:100%; height:470px;"></div> <noscript> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-s1_dilution-480.webp 480w,/assets/img/expdisc-s1_dilution-800.webp 800w,/assets/img/expdisc-s1_dilution-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-s1_dilution.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </noscript> </figure> <p>And here’s the catch that sets up everything after: the best subset is <em>different for every target</em>. Yet a plain correlation ranking already recovers 96% of the oracle’s gain. So on a dataset like this, where every assay is co-measured, so correlation is a clean, trustworthy signal, a twenty-line heuristic gets you <em>most</em> of the way to optimal. Which raises the real question: <strong>is there anything left for an agent to do?</strong> Two places it might. At the coldest start ($n=5$), where correlation is estimated from almost no data and turns noisy. And on datasets where assays <em>don’t</em> share molecules, so a correlation can’t be computed at all. Both are exactly where we look next.</p> <h2 id="our-agent">Our agent</h2> <p>Our first agent is deliberately simple. It sees the candidate assays with their stats and metadata, and has two moves: <code class="language-plaintext highlighter-rouge">evaluate</code> a proposed source set — we run the transfer for real and hand back its held-out score — and <code class="language-plaintext highlighter-rouge">finalize</code> a set with a short, chemist-readable reason. It evaluates a few candidates, then commits.</p> <p>Here is one such run, verbatim — the metabolism target HLM CLint with just five labels:</p> <style>.react-trace{border:1px solid rgba(128,128,128,.35);border-radius:10px;padding:1.1em 1.2em;margin:1.6em 0;font-size:.9rem;line-height:1.5;background:rgba(128,128,128,.04)}.react-trace ol{margin:0;padding-left:1.4em}.react-trace li{margin:.55em 0;padding-left:.2em}.react-trace .act{font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;font-size:.83rem;color:#7c3aed;font-weight:600}.react-trace .obs{font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;font-size:.82rem;opacity:.85}.react-trace .err{color:#c0392b}.react-trace .th{opacity:.9}.react-trace .cap{margin-top:.9em;font-size:.8rem;opacity:.7}@media(prefers-color-scheme:dark){.react-trace .act{color:#a78bfa}.react-trace .err{color:#ff7a6b}}</style> <div class="react-trace"> <ol> <li><span class="th">Starting point: no-transfer floor</span> <span class="obs">→ val ρ 0.11.</span></li> <li><span class="th">"Try a mechanistically related metabolism assay plus a physicochemical property."</span> → <span class="act">evaluate {MLM CLint, LogD}</span></li> <li><span class="th">"Is the metabolism assay enough on its own?"</span> → <span class="act">evaluate {MLM CLint}</span></li> <li><span class="th">"Swap the physicochemical partner for solubility?"</span> → <span class="act">evaluate {MLM CLint, KSOL}</span></li> <li><span class="th">"Does a third source help?"</span> → <span class="act">evaluate {MLM CLint, LogD, KSOL}</span></li> <li><span class="th">"Re-confirm the leader."</span> → <span class="act">evaluate {MLM CLint, LogD}</span></li> <li><span class="act">finalize {MLM CLint, LogD}</span> — <span class="th">"mechanistically related metabolism assay and a physicochemical property with good coverage and moderate correlation, yielding the highest validation score."</span> <span class="obs">val ρ 0.63 → held-out test ρ 0.50.</span></li> </ol> <div class="cap">The simple agent probes a handful of source sets — single vs. paired sources, swapping the physicochemical partner, testing a third — then commits to the metabolism sister assay (MLM CLint) and lipophilicity (LogD). It was handed no chemistry, yet the pick is textbook.</div> </div> <p>And the choices stay chemically legible across all nine targets: the two cross-species clearance assays (HLM and MLM CLint) pick each other, the two Caco-2 permeability assays do the same, and the three tissue-binding endpoints form a mutually-selecting cluster — textbook ADMET relationships, rediscovered.</p> <p>On OpenADMET it answers the first question cleanly. The agent <strong>reaches the oracle</strong>, <strong>beats naive all-source pooling decisively</strong> ($p &lt; 10^{-4}$), and <strong>matches the correlation heuristic</strong>. Its margin over the heuristic concentrates exactly at the coldest start ($n=5$), where a correlation estimated from five points is least trustworthy.</p> <figure class="expdisc-figure"> <div class="expdisc-chart" id="expdisc-fig-methods" style="width:100%; height:480px;"></div> <noscript> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-s1_method_lines-480.webp 480w,/assets/img/expdisc-s1_method_lines-800.webp 800w,/assets/img/expdisc-s1_method_lines-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-s1_method_lines.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </noscript> </figure> <p>Where the heuristic has enough co-measured points to trust its correlation, reasoning adds little and the two converge. But at the $n=5$ cold start, where a five-point correlation is mostly noise, the agent is already ahead: it wins precisely where the cheap signal doesn’t yet exist.</p> <p>To further showcase this, we evaluate on a dataset where correlation is unreliable by design. On the Therapeutics Data Commons benchmark <d-cite key="huang2021tdc"></d-cite> (22 ADMET tasks, mostly <em>not</em> co-measured, so correlation is often undefined), the agent’s edge over the heuristic <em>grows with data</em>: its win-rate climbs from 55% to 82% as you give it more shots, because its <code class="language-plaintext highlighter-rouge">evaluate</code> signal sharpens while a static correlation statistic does not. Same agent; the difference is whether the cheap baseline is fixable. On the nine regression tasks, where small-sample correlation is noisiest, the agent leads every baseline at every budget. Naive pooling or random selection sink <em>below</em> no-transfer, the same collapse we saw once OpenADMET’s source pool grew.</p> <figure class="expdisc-figure"> <div class="expdisc-chart" id="expdisc-fig-tdc-baselines" style="width:100%; height:470px;"></div> <noscript> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-tdc_baselines-480.webp 480w,/assets/img/expdisc-tdc_baselines-800.webp 800w,/assets/img/expdisc-tdc_baselines-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-tdc_baselines.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </noscript> </figure> <p>One test pins down <em>where</em> that lead comes from. The agent reads two kinds of information — the correlation statistics and the assays’ text descriptions — so we removed each in turn. Strip the descriptions and it falls back to the plain correlation heuristic (worst at $n=50$, where that heuristic is weakest); strip the correlations instead and it keeps almost all of its edge. What the reasoning adds is the biology in the metadata, not the statistics.</p> <figure class="expdisc-figure"> <div class="expdisc-chart" id="expdisc-fig-tdc" style="width:100%; height:470px;"></div> <noscript> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-tdc_scaling-480.webp 480w,/assets/img/expdisc-tdc_scaling-800.webp 800w,/assets/img/expdisc-tdc_scaling-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-tdc_scaling.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </noscript> </figure> <p>So far the agent has made a single call — <em>what to transfer from</em> — and made it well, pulling ahead exactly where the cheap statistics run out. But source selection is one decision in a campaign that has many. Can the same reasoning drive the rest?</p> <h2 id="how-far-can-one-agent-go">How far can one agent go?</h2> <p>A real campaign is a chain of decisions, not one: past <em>which</em> assays to borrow from, someone must choose <em>how many</em> compounds to label and <em>which</em> ones. We hand both to the agent — one it aces, one it flubs.</p> <p><strong>Can it run the whole pipeline?</strong> By <em>whole pipeline</em> we mean the three decisions taken together — which source assays to pretrain on, how many new target compounds to label ($n$), and which specific compounds to pick — made inside a single loop and judged by one held-out test evaluation at the very end. We give this to one agent running a <strong>reason–act loop</strong> (the ReAct pattern <d-cite key="yao2023react"></d-cite>: the model alternates a written <em>thought</em> with a concrete <em>action</em>, then uses what it observes to choose the next move). Its actions come from a fixed toolset: <code class="language-plaintext highlighter-rouge">inspect_data</code> and <code class="language-plaintext highlighter-rouge">source_stats</code> to orient itself (coverage, value ranges, correlations, the no-transfer floor), <code class="language-plaintext highlighter-rouge">evaluate</code> to run one full configuration — sources, acquisition method, $n$ — and read back its <em>validation</em> score, <code class="language-plaintext highlighter-rouge">run_python</code> for a persistent sandbox where it can test its own hypotheses (compute a statistic, try a descriptor), and <code class="language-plaintext highlighter-rouge">finalize</code> to commit a configuration plus a chemist-facing report. A run takes 9–14 of these steps; the test set is revealed exactly once, on whatever it finalizes.</p> <p>Here is one run, verbatim — the agent’s own thoughts and actions on the tissue-binding target MBPB, lightly trimmed:</p> <div class="react-trace"> <ol> <li><span class="act">inspect_data</span> · <span class="act">source_stats</span> — <span class="th">orient: coverage, value range, no-transfer floor, correlations to every candidate.</span></li> <li><span class="th">"MGMB correlates 0.90 with MBPB and shares 216 molecules — a valuable source."</span> → <span class="act">evaluate {MGMB}</span> <span class="obs">→ val ρ 0.51</span></li> <li><span class="th">"Add the next-best, MPPB (corr 0.61)."</span> → <span class="act">evaluate {MGMB, MPPB}</span> <span class="obs">→ val ρ 0.74</span></li> <li><span class="th">"Are they complementary or redundant?"</span> → <span class="act">run_python: train['MGMB']['smiles'].corr(...)</span> <span class="err">→ KeyError</span></li> <li><span class="th">"That key doesn't exist; use the label columns directly."</span> → <span class="act">run_python: train['MGMB'].corr(train['MPPB'])</span> <span class="obs">→ 0.46 — complementary, not redundant.</span></li> <li><span class="th">"Add LogD (corr 0.51, 961 shared)."</span> → <span class="act">evaluate {MGMB, MPPB, LogD}</span> <span class="obs">→ val ρ 0.79</span></li> <li><span class="th">"Now tune the knobs."</span> → sweep acquisition and budget: <span class="act">random</span> <span class="obs">(lower)</span>, <span class="act">farthest_first n=50</span> <span class="obs">(overfits, drops)</span>, <span class="act">kmedoids n=25</span> <span class="obs">→ val ρ 0.81, the best yet.</span></li> <li><span class="act">finalize</span> — commit {MGMB, MPPB, LogD}, kmedoids, n=25, with a written rationale. <span class="obs">Held-out test ρ 0.67.</span></li> </ol> <div class="cap">A single autoresearch run on MBPB. The agent reasons from the correlation table, builds the source set incrementally, checks its own hypothesis in the sandbox — hitting and recovering from a real error — then tunes acquisition and budget before committing. Every step is logged as natural language a chemist can audit.</div> </div> <p>On the three sparsest endpoints — the genuinely label-starved ones, 222 to 1,300 labels — it reaches <strong>96% of the brute-force oracle</strong> and <strong>+56% over no transfer</strong>, beating the floor on all nine runs; the win concentrates where labels are scarcest. But it does not <em>beat</em> the strong human baseline. Our expert <strong>greedy pipeline</strong> — rank the candidate sources by their correlation to the target, add them one at a time as long as the validation score keeps rising, stop when it doesn’t — lands in the same place: averaged over the three targets the agent scores ρ≈0.69 against greedy’s ρ≈0.70, a tie well inside the seed-to-seed noise.</p> <figure class="expdisc-figure"> <div class="expdisc-chart" id="expdisc-fig-autoresearch" style="width:100%; height:460px;"></div> <noscript> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-autoresearch_vs_oracle-480.webp 480w,/assets/img/expdisc-autoresearch_vs_oracle-800.webp 800w,/assets/img/expdisc-autoresearch_vs_oracle-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-autoresearch_vs_oracle.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </noscript> </figure> <p>The claim is automation, not dominance: one agent that also tunes the knobs a human would, matching the expert and nearly reaching the oracle, while writing down its reasoning. And on the <em>hardest</em> target — MPPB, the lowest floor (ρ≈0.32 from scratch) — it does edge past greedy, ρ≈0.68 to 0.66. The reasoning earns the most where transfer is hardest.</p> <p><strong>Can it pick which molecules to measure first (acquisition)?</strong> This is the neighbouring question, and arguably the more valuable one to a chemist: with <em>zero</em> labels on a new target, which molecules do you measure first to bootstrap the assay? Acquisition was already one knob in the pipeline above, but buried there — the agent just picked a named algorithm (<code class="language-plaintext highlighter-rouge">kmedoids</code>, <code class="language-plaintext highlighter-rouge">farthest_first</code>, …) to sub-sample a pool that <em>already had</em> labels, and its effect was tangled up with the source and budget choices in a single score. Here we strip it out and make it harder: no labels at all, and the agent must reason about <em>individual</em> molecules from their structure rather than call a canned method — precisely the setting where a model’s chemical knowledge should pay off, if it ever does. We build it a per-molecule recommender — it reads the other assays’ values plus RDKit descriptors and names specific molecules, with a reason for each (“unique scaffold and low LogD”, “high Caco-2 efflux”, “similar to m12, skip it”).</p> <p>We try the acquisition agent three ways, each given more to work with: one <strong>allocates a labeling budget across chemical clusters</strong>, one <strong>names individual molecules</strong> from a shortlist with a reason for each, and one gets k-medoids’<d-footnote>k-medoids is a clustering method that picks <em>k</em> actual data points as cluster centres, so the chosen molecules are real, maximally-spread representatives of the pool. With no target labels to learn from, spreading the measurements evenly across chemical space — covering the range of structures instead of clumping — is the sensible default, which is why it is a strong baseline here.</d-footnote> <em>exact</em> input — the full pool embedding — with the heuristic itself as a callable tool. At the true cold start ($n=5$), representative sampling is what matters and <strong>k-medoids leads</strong>; no agent variant beats it. Past a handful of picks the field just converges — the gaps fall inside the seed-to-seed noise, and on a set as chemically homogeneous as OpenADMET even <em>random</em> selection catches up, edging ahead by $n=25$. Once you have more than a few molecules, coverage is easy and how you choose stops mattering.</p> <figure class="expdisc-figure"> <div class="expdisc-chart" id="expdisc-fig-coldstart" style="width:100%; height:470px;"></div> <noscript> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-coldstart_acquisition-480.webp 480w,/assets/img/expdisc-coldstart_acquisition-800.webp 800w,/assets/img/expdisc-coldstart_acquisition-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-coldstart_acquisition.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </noscript> </figure> <p>The lesson is that <strong>k-medoids is a genuinely good heuristic</strong> for this job. Spreading picks to cover the embedding is close to the right objective for “which molecules are worth labeling,” and it costs nothing. Reasoning over each molecule’s name and descriptors — the thing the LLM adds — lands <em>below</em> that at the cold start: guessing which compound is informative from its structure is harder than simply covering the space, and even handing the agent k-medoids as a tool only lets it match the heuristic, never beat it. Could a model with deeper medicinal-chemistry knowledge do better — flag a redundant scaffold the geometry misses? Maybe; on this data it didn’t. (The per-molecule agent still earns its place in the demo as the <em>interpretable</em> layer — every pick comes with an auditable chemical reason — but that’s transparency, not accuracy.)</p> <h2 id="conclusion-demo-and-code">Conclusion, demo and code</h2> <p>On transfer learning, the agent helps most with the judgment calls: choosing which assays to transfer from, and running the whole selection-and-labeling pipeline on its own. It picks sources that make chemical sense, routing a half-life target to clearance assays and a protein-binding target to lipophilicity without being told any chemistry, and it holds up best where information is thinnest: only a handful of labels, or candidate assays that share no molecules with the target so the usual correlation can’t be computed. Once there is enough data, though, the cheap baselines catch up. Given enough co-measured labels to estimate a reliable correlation, a twenty-line heuristic selects sources about as well as the agent, and a hand-tuned greedy procedure runs the pipeline about as well; both come close to an oracle that is allowed to peek at the test set. The agent’s real advantage is in the low-data regime, not everywhere.</p> <p>On data acquisition, it is no help when a simple algorithm already covers the problem. Deciding which molecules to measure first is really just spreading the picks across chemical space, and k-medoids does that directly; past the first few molecules even a random choice does as well. Giving the agent that same embedding, plus k-medoids as a callable tool, changes nothing: it mostly just calls k-medoids. So the rule we take away is a plain one. Use the model for the parts that call for reading and judgment, and leave the well-posed numerical problems to the algorithms that already solve them.</p> <p>Try the agent <a href="https://admet-demo-diffalign.2.rahtiapp.fi/">here</a>: it suggests which molecules to measure next and explains each pick. The <a href="https://github.com/NajwaLaabid/assay-transfer-agent">code</a> is on GitHub.</p> <p><em>Built with Halidu Abdulai (Åbo Akademi University), Anirudh Jain (Orion), Ilari Tulkki (CSC), and Gerardo Gonzalez (Silo AI) at the Exponential Discovery in Life Sciences hackathon.</em></p> <script src="/assets/js/expdisc-charts.js"></script>]]></content><author><name>Najwa Laabid</name></author><category term="research"/><category term="agents"/><category term="transfer-learning"/><category term="drug-discovery"/><summary type="html"><![CDATA[Modern drug discovery leans hard on prediction: rather than measure every property of every candidate in the lab, you train models to guess them in silico and only run the measurements you have to. When a property has too few labels to learn from, the standard trick is to borrow what a model already picked up from a related, better-measured property, also known as transfer learning. The catch is deciding which property to borrow from, and that is exactly the kind of judgement call you might hope an AI agent could make. Here’s where it can, and where a twenty-line heuristic does the job just as well.]]></summary></entry></feed>