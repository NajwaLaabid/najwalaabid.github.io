<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://najwalaabid.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://najwalaabid.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-15T17:09:05+00:00</updated><id>https://najwalaabid.github.io/feed.xml</id><title type="html">blank</title><subtitle>I write about my work and other topics I like in ML research. </subtitle><entry><title type="html">Where do LLM agents help with property prediction?</title><link href="https://najwalaabid.github.io/blog/2026/where-llm-agents-help-drug-discovery/" rel="alternate" type="text/html" title="Where do LLM agents help with property prediction?"/><published>2026-06-24T00:00:00+00:00</published><updated>2026-06-24T00:00:00+00:00</updated><id>https://najwalaabid.github.io/blog/2026/where-llm-agents-help-drug-discovery</id><content type="html" xml:base="https://najwalaabid.github.io/blog/2026/where-llm-agents-help-drug-discovery/"><![CDATA[<p>The purpose of this project is to test whether an agentic loop can make a meaningful difference in the property prediction stage of drug discovery. We test how well an agent can choose cheap properties to pretrain on in order to make predicting expensive ones more label-efficient. We then ask whether the same agentic design extends to data acquisition (letting the agent choose which specific molecules to measure) and to full auto-research automation (combining property selection and molecule acquisition). Our main observation is that the agent’s reasoning pays off when simpler heuristics are not reliable (e.g. too few samples to compute correlation). When a good heuristic exists, the agent ties with it at best, and can even lose by reaching for needlessly complex tools.</p> <h2 id="background-on-property-prediction-for-molecules">Background on property prediction for molecules</h2> <p>In drug development, candidate drugs must be tested for how they behave in the body: how they get <strong>Absorbed</strong>, <strong>Distributed</strong>, <strong>Metabolized</strong>, and <strong>Excreted</strong>, and how <strong>Toxic</strong> they are, summarized in the acronym ADMET <d-cite key="waterbeemd2003admet"></d-cite>. Assessing these behaviors means measuring chemical and biological properties called <strong>endpoints</strong>, each obtained through an experimental protocol called an <strong>assay</strong>. In a dataset of molecular properties, each feature column typically corresponds to one assay targeting a single endpoint, though an endpoint may be measured by several assays. We use assay, endpoint, and property interchangeably throughout this blog. Table 1 lists common endpoints per ADMET stage from the two datasets in this study, OpenADMET and TDC (details in the setup).</p> <table> <thead> <tr> <th>ADMET stage</th> <th>The question it answers</th> <th>Common endpoints (<strong>bold</strong> = OpenADMET · <em>italic</em> = TDC)</th> </tr> </thead> <tbody> <tr> <td><strong>A</strong>bsorption</td> <td>Does it get into the bloodstream?</td> <td><strong><em>solubility (KSOL)</em></strong>, <strong><em>permeability (Caco-2)</em></strong>, <strong>efflux (Caco-2 ratio)</strong>, <strong><em>lipophilicity (LogD)</em></strong>, <em>intestinal absorption (HIA)</em>, <em>P-gp inhibition</em>, <em>oral bioavailability</em></td> </tr> <tr> <td><strong>D</strong>istribution</td> <td>Where in the body does it go?</td> <td><strong><em>protein binding (MPPB / MBPB / MGMB; PPBR)</em></strong>, <em>volume of distribution (VDss)</em>, <em>blood–brain barrier (BBB)</em></td> </tr> <tr> <td><strong>M</strong>etabolism</td> <td>How fast is it broken down?</td> <td><strong><em>microsomal clearance (HLM, MLM CLint)</em></strong>, <em>hepatocyte clearance</em>, <em>CYP 2C9 / 2D6 / 3A4 inhibition &amp; substrate</em></td> </tr> <tr> <td><strong>E</strong>xcretion</td> <td>How is it cleared from the body?</td> <td><em>half-life</em>, renal clearance</td> </tr> <tr> <td><strong>T</strong>oxicity</td> <td>Could it cause harm?</td> <td><em>LD50</em>, <em>hERG (cardiac)</em>, <em>Ames (mutagenicity)</em>, <em>DILI (liver injury)</em></td> </tr> </tbody> </table> <p class="caption"><strong>Table 1.</strong> Common ADMET endpoints by stage; <strong>bold</strong> = OpenADMET, <em>italic</em> = TDC.</p> <p>The challenge in property-based molecule screening is that assays differ vastly in complexity and cost. Testing how well a molecule dissolves in water (aqueous solubility) needs only a dish of water and a small quantity of each molecule. Testing toxicity (e.g. median lethal dose, or LD50) instead requires dosing animals one at a time and monitoring them over timed intervals <d-cite key="kerns2016druglike"></d-cite>. When a campaign screens hundreds of molecules iteratively, the more resource-intensive endpoints become a real bottleneck.</p> <p>An obvious workaround is data-driven learning: train a model on previous assay measurements to predict those of future molecules (<em>in silico</em> measurement). But such models are notoriously data-hungry, and collecting enough labels to train expensive assays is itself costly. If we can instead <em>transfer</em> what a model learns from other properties, we may not need much data to train it on the expensive ones. This is what <em>transfer learning</em> does: pretraining a model on related properties to boost its performance in data-scarce spaces <d-cite key="pan2010survey"></d-cite>.</p> <p>The catch is that what a model learns within one property may not predict behavior in a very different one. For example, aspirin and ibuprofen are both common painkillers that the gut absorbs about equally well, yet the body clears them in completely different ways: aspirin is broken down almost instantly by esterases into salicylate, while ibuprofen is oxidized more slowly by a liver enzyme (CYP2C9). A model that learned how well molecules like these get absorbed would tell you nothing about how fast they are metabolized. In our own experiments, predicting LogD from just 5 labels is essentially a coin-flip (ρ≈0); pretraining on mouse plasma protein binding first lifts it to ρ≈0.57, but pretraining on Caco-2 efflux pushes it <em>below</em> the no-transfer floor (ρ≈−0.09).</p> <figure class="expdisc-figure"> <div class="expdisc-chart" id="expdisc-fig-negtransfer" style="width:100%; height:460px;"></div> <noscript> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-negative_transfer-480.webp 480w,/assets/img/expdisc-negative_transfer-800.webp 800w,/assets/img/expdisc-negative_transfer-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-negative_transfer.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </noscript> <figcaption class="caption">Transfer is only as good as its source: pretraining a 5-label LogD model on mouse plasma protein binding lifts a coin-flip (ρ≈0) to ρ≈0.57, while Caco-2 efflux drags it <em>below</em> the no-transfer floor (ρ≈−0.09).</figcaption> </figure> <p>Choosing which properties to pretrain on thus matters, and usually relies on cheap metrics like correlation or chemical-space similarity between assays, transferability scores developed for ranking pretrained models <d-cite key="you2021logme"></d-cite>, chemical knowledge, and the intuition of experienced medicinal chemists.</p> <p>LLM agents have already been handed real chemistry to do: ChemCrow gives one a set of expert tools to call, and Coscientist lets one plan and run reactions on a robotic platform</p> <d-cite key="bran2024chemcrow,boiko2023coscientist"></d-cite> <p>. We ask a narrower, more skeptical question: whether the agent’s reasoning beats cheap heuristics a chemist would otherwise reach for. Specifically, we ask 3 questions: 1) does an agent outperform heuristics?, 2) can it reason from chemical knowledge when those heuristics are not available?, 3) and how far can we automate the property prediction pipeline with an advanced agentic program? The <a href="https://admet-demo-diffalign.2.rahtiapp.fi/">interactive demo</a> and the <a href="https://github.com/NajwaLaabid/assay-transfer-agent">code</a> are online if you’d rather see the agent at work first; otherwise, read on for how we got there.</p> <h2 id="the-setup">The setup</h2> <p>We test all of this on two datasets. <strong>OpenADMET</strong> <d-cite key="openadmet2025expansionrx"></d-cite> measures different endpoints for the same molecules, so we can compute heuristics like correlation and cooccurrence when selecting pretraining assays. <strong>TDC</strong> (Therapeutics Data Commons) <d-cite key="huang2021tdc"></d-cite> has 22 endpoints, each curated on its own molecule collection, with median assay pairs sharing 2% of their combined molecules. What TDC offers instead is much richer metadata. We use OpenADMET to study selection when the cheap signal is trustworthy, and TDC to test transfer when it is not. The data distribution of both datasets is summarized below.</p> <figure class="expdisc-figure"> <div class="expdisc-chart" id="expdisc-fig-coverage" style="width:100%; height:560px;"></div> <noscript> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-data_coverage-480.webp 480w,/assets/img/expdisc-data_coverage-800.webp 800w,/assets/img/expdisc-data_coverage-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-data_coverage.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </noscript> <figcaption class="caption">Assay coverage across the two datasets: OpenADMET co-measures its endpoints on the same molecules, so correlation is computed on solid ground, whereas TDC's 22 assays each bring their own molecules and barely overlap, leaving metadata as the more reliable signal.</figcaption> </figure> <p>Our predictor is a simple MLP (2 layers, 512 then 128 units, with ReLU and dropout) with a detachable head per property: we train the shared early layers (the <em>trunk</em>) on all selected pretraining assays at once, but each assay gets its own final linear layer. It takes molecular fingerprints (2048-bit ECFP) plus a set of simple RDKit descriptors as input, and outputs a continuous value for the property, trained to minimize the mean squared error against the true values. At transfer time we freeze the trunk and fit a fresh head on the few target labels. This is among the simpler models we could have chosen.<d-footnote> Tasks like this often use message-passing graph neural networks like Chemprop <d-cite key="yang2019chemprop"></d-cite> or larger pretrained molecular foundation models <d-cite key="ross2022molformer"></d-cite>.</d-footnote> Since the goal is to compare an agent against baselines, what matters is a fixed predictor across experiments.</p> <p>On the infrastructure side, the predictor is written in PyTorch and every experiment runs as a plain Python script on LUMI, CSC’s GPU supercomputer (one AMD GPU per job inside a ROCm container). The agent’s reasoning calls go through <a href="https://zenodo.org/records/17305233">Aitta</a>, CSC’s LUMI AI Factory inference service, an OpenAI-compatible API serving open-weight LLMs on LUMI. Unless noted otherwise, the agent is Llama-3.3-70B-Instruct. The agentic loop itself is covered in the next section.</p> <p>In each experiment we single out one assay as the target and let the model see only a handful of its labels (namely, $n \in {5, 10, 25, 50}$ molecules), while it pretrains on other assays chosen by whichever selection method we are testing. Both stages stay strictly on the training split: the trunk is pretrained on the full training labels of the selected assays, then a fresh head is fit on the $n$ sampled target labels with the trunk frozen. The held-out test set is never touched until scoring, where we evaluate with the Spearman rank correlation between predictions and true values.<d-footnote>Spearman measures whether the model orders molecules correctly rather than how close it lands on the exact value, which is what matters when the goal is to prioritize which molecules to make or measure next. It runs from −1 (reversed) to 1 (perfect order).</d-footnote></p> <h2 id="agents-help-where-heuristics-fail">Agents help where heuristics fail</h2> <p>We run the following baselines to test how well we can do without the LLM. First, the <strong>no transfer</strong> baseline trains the predictor on $n=5$ observations from each assay, while the <strong>ceiling</strong> run trains on all available observations as an upper bound. We also try pretraining on two <strong>random</strong>ly selected assays, on <strong>all</strong> assays, and on the most <strong>correlated</strong> assays. On OpenADMET that correlation is the plain one: candidate and target are measured on the same molecules, so we correlate their labels directly. On TDC, where assays share few molecules, we fit a model on the candidate assay’s labels, have it predict on the target’s molecules, and correlate those predictions against the target labels. Finally, since OpenADMET only has 9 endpoints, we also run an <strong>oracle</strong> experiment where we try every possible combination of 2 assays for each of the 9 endpoints (28 pairs per endpoint, 252 in total), to get the ground truth evaluation of how well transfer learning can perform on this dataset. We settle on two pretraining assays because the ablation below shows that transfer performance peaks at roughly two well-chosen sources and then degrades as more are added, with each extra assay muddying the shared trunk with weakly related signal.</p> <figure class="expdisc-figure"> <div class="expdisc-chart" id="expdisc-fig-dilution" style="width:100%; height:470px;"></div> <noscript> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-s1_dilution-480.webp 480w,/assets/img/expdisc-s1_dilution-800.webp 800w,/assets/img/expdisc-s1_dilution-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-s1_dilution.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </noscript> <figcaption class="caption">Transfer performance peaks at roughly two well-chosen sources, then degrades as each added assay muddies the shared trunk with weakly related signal, which is why we cap selection at two.</figcaption> </figure> <p>For the agents, we keep the loop deliberately simple and change only what the agent is allowed to reason over. The loop is the same throughout. Each turn the agent proposes a candidate set of source assays; we score that choice on a held-out validation split (never the test set) and hand the number back. After a few rounds, or once its step budget runs out, it commits to a final set and writes a short justification a chemist could read. What differs between our two agent types is the information in front of it. The <strong>correlation-only</strong> agent sees only the numbers: each candidate assay’s coverage and its correlation with the target on shared molecules. This makes it essentially a language model wrapped around the same heuristic our baselines use. The <strong>metadata-aware</strong> agent additionally sees the biology of each assay: which ADMET stage it belongs to, its units, and a short description of what it measures. It can therefore reason about whether two assays are mechanistically related, not just numerically correlated. Where correlation is reliable (OpenADMET) the two agents should behave alike; where it breaks down (TDC) the metadata-aware agent has something to work with that the plain heuristic does not. This metadata-aware agent, which sees both the correlation numbers and the assay biology, is what we call the <strong>full agent</strong>, and it is our default in every experiment unless stated otherwise. Below we share a description of the agentic loop and logs from one of the agent’s runs to illustrate the loop in action.</p> <div class="l-page"> <div style="display:flex; gap:1.6rem; flex-wrap:wrap; align-items:stretch; margin:1.4rem 0;"> <div class="loop-imgcard"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-agent_loop_basic-480.webp 480w,/assets/img/expdisc-agent_loop_basic-800.webp 800w,/assets/img/expdisc-agent_loop_basic-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-agent_loop_basic.png" class="img-fluid" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="react-trace" style="flex:1 1 0; min-width:300px; margin:0;"> <div class="trace-head"><strong>Agent run logs</strong>: the target is <strong>HLM CLint</strong> (OpenADMET; human liver microsomal clearance, a metabolism endpoint) with <strong>10 labels</strong>; candidate sources: the other OpenADMET assays.</div> <ol> <li><span class="th">Starting point: no-transfer floor</span> <span class="obs">→ val ρ 0.11.</span></li> <li><span class="th">"Try a mechanistically related metabolism assay plus a physicochemical property."</span> → <span class="act">evaluate {MLM CLint, LogD}</span></li> <li><span class="th">"Is the metabolism assay enough on its own?"</span> → <span class="act">evaluate {MLM CLint}</span></li> <li><span class="th">"Swap the physicochemical partner for solubility?"</span> → <span class="act">evaluate {MLM CLint, KSOL}</span></li> <li><span class="th">"Does a third source help?"</span> → <span class="act">evaluate {MLM CLint, LogD, KSOL}</span></li> <li><span class="th">"Re-confirm the leader."</span> → <span class="act">evaluate {MLM CLint, LogD}</span></li> <li><span class="act">finalize {MLM CLint, LogD}</span> — <span class="th">"mechanistically related metabolism assay and a physicochemical property with good coverage and moderate correlation, yielding the highest validation score."</span> <span class="obs">val ρ 0.63 → held-out test ρ 0.40, against a no-transfer floor of 0.17.</span></li> </ol> <div class="cap">The agent immediately chooses a mechanistically related assay (i.e. another clearance), and spends its budget looking for an assay to supplement it.</div> </div> </div> </div> <p>On OpenADMET, transferring from any assay makes a big difference, especially with only 5 labels: the no-transfer baseline is essentially useless (ρ≈0.05), while pretraining on almost any source already surpasses 0.2 Spearman. Because molecules here are co-measured across assays, the cheap correlation heuristic is strong, landing within a few points of the brute-force oracle. The agent matches that heuristic, and its one clear edge over it comes at the coldest start ($n=5$), where a five-point correlation is mostly noise. As we will see later in the post, this observation of tying with simpler heuristics when they are reliable holds in other experiments as well. The agent’s picks are also chemically meaningful: HLM and MLM CLint (the two cross-species clearance assays) select each other, and the Caco-2 permeability assays do the same.</p> <figure class="expdisc-figure"> <div class="expdisc-chart" id="expdisc-fig-methods" style="width:100%; height:480px;"></div> <noscript> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-s1_method_lines-480.webp 480w,/assets/img/expdisc-s1_method_lines-800.webp 800w,/assets/img/expdisc-s1_method_lines-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-s1_method_lines.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </noscript> <figcaption class="caption">On OpenADMET any transfer beats the near-useless no-transfer floor (ρ≈0.05); with correlation reliable here, the agent matches the cheap heuristic and only pulls clearly ahead at the coldest start (n=5), where a five-point correlation is mostly noise.</figcaption> </figure> <p>TDC is the harder case: with each assay curated on its own molecules, the correlation heuristic has little solid ground to stand on, and even the generous predictive proxy we hand it does not lift it far. Here transferring from the wrong assay actively hurts: random and all-source pooling both fall below the no-transfer baseline (the grey line) from $n=25$ on, making a badly chosen source worse than none at all. The agent still leads every baseline at every budget (ρ≈0.30 vs 0.25 for no-transfer at $n=50$), because it has two things the heuristic lacks: assay metadata to reason about which properties are mechanistically related, and a validation signal that sharpens as more target labels arrive.</p> <figure class="expdisc-figure"> <div class="expdisc-chart" id="expdisc-fig-tdc-baselines" style="width:100%; height:470px;"></div> <noscript> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-tdc_baselines-480.webp 480w,/assets/img/expdisc-tdc_baselines-800.webp 800w,/assets/img/expdisc-tdc_baselines-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-tdc_baselines.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </noscript> <figcaption class="caption">On TDC a badly chosen source is worse than none: random and all-source pooling fall below the no-transfer floor (grey) from n=25 on, while the full agent leads every baseline at every budget.</figcaption> </figure> <p>To see which of these matters more, we ablate the agent’s inputs. With no metadata, the agent loses its edge and collapses back to the heuristic by $n=50$; given metadata alone, it recovers almost all of the full agent’s performance. So on these barely-overlapping tasks it is the chemical reasoning over assay descriptions, not the correlation numbers, that carries the agent.</p> <figure class="expdisc-figure"> <div class="expdisc-chart" id="expdisc-fig-tdc" style="width:100%; height:470px;"></div> <noscript> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-tdc_scaling-480.webp 480w,/assets/img/expdisc-tdc_scaling-800.webp 800w,/assets/img/expdisc-tdc_scaling-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-tdc_scaling.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </noscript> <figcaption class="caption">The agent's edge over the correlation heuristic widens as target labels accumulate: its win-rate climbs from 55% of the oracle at n=10 to 82% at n=50, as the validation signal it optimizes sharpens while the static heuristic does not.</figcaption> </figure> <p>So far the agent has chosen which assays to transfer from. Next we ask whether it can also choose which molecules to measure for a new endpoint, and then how far full automation can go with our current setup.</p> <h2 id="can-agents-choose-specific-molecules-to-acquire">Can agents choose specific molecules to acquire?</h2> <p>When an endpoint is expensive to measure, which molecules get labeled first decides how efficiently the chemical space is covered. This is the acquisition problem, and we ask whether an agent can handle it too. Unlike source selection, there is no evaluate-and-revise loop here: any validation run would itself spend labels on the very endpoint we are trying to measure sparingly, so comparing candidate sets defeats the purpose. The agent gets a single shot to choose which molecules to measure. For this experiment, we try 3 agent loops, differing only in what each one sees.</p> <figure class="expdisc-figure" style="max-width:600px; margin:1.6rem auto;"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-acquisition_loop-480.webp 480w,/assets/img/expdisc-acquisition_loop-800.webp 800w,/assets/img/expdisc-acquisition_loop-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-acquisition_loop.png" class="img-fluid" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </figure> <p>So can an agent help us choose which molecules to acquire? Yes, but only about as well as a k-medoids algorithm<d-footnote>k-medoids <d-cite key="kaufman1990pam"></d-cite> is a clustering method that picks <em>k</em> actual data points as cluster centers, so the chosen molecules are real, maximally-spread representatives of the pool. With no target labels to learn from, spreading the measurements evenly across chemical space is the sensible default, which is why it is a strong baseline here. (We use the cheap variant: k-means, then the pool molecule nearest each centroid.)</d-footnote>, or the more elaborate D-optimal design<d-footnote>D-optimal design comes from optimal experimental design</d-footnote></p> <d-cite key="fedorov1972theory,atkinson2007optimum"></d-cite> <p>: it greedily measures the molecule the current fit is least certain about, which is also the one that most increases the determinant of the information matrix.&lt;/d-footnote&gt;. When handed a sandbox and a set of tools, the agent does not design an acquisition strategy of its own, instead it calls one of the tools and finalizes its picks. For both datasets, the agent lands wherever that tool (and its corresponding baseline) lands. When a simple algorithm already captures the signal the agent has nothing of its own to add, and on datasets this small a well-chosen algorithm matches or beats it.</p> <div class="l-page"> <figure class="expdisc-figure"> <div class="expdisc-chart" id="expdisc-fig-coldstart-combined" style="width:100%; height:500px;"></div> <noscript> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-coldstart_combined-480.webp 480w,/assets/img/expdisc-coldstart_combined-800.webp 800w,/assets/img/expdisc-coldstart_combined-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-coldstart_combined.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </noscript> <figcaption class="caption">Cold-start molecule selection (label-free; mean test ρ over 9 regression targets, ±1 SEM). Representative sampling wins: k-medoids leads on OpenADMET (left), D-optimal design on TDC (right), and by n=25 nothing clearly beats random. </figcaption> </figure> </div> <h2 id="full-automation-are-we-there-yet">Full automation, are we there yet?</h2> <p>Lastly, we ask how an agent would handle the decisions of the property-prediction pipeline together: which assays to transfer from, and which specific molecules to acquire. We give the agent maximum freedom here, following a reason–act design <d-cite key="yao2023react"></d-cite> in which it has access to a set of tools, can write its own Python scripts, and decides what to measure next, as shown in the figure below.</p> <div class="l-page"> <div style="display:flex; gap:1.6rem; flex-wrap:wrap; align-items:stretch; margin:1.4rem 0;"> <div class="loop-imgcard"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-autoresearch_loop-480.webp 480w,/assets/img/expdisc-autoresearch_loop-800.webp 800w,/assets/img/expdisc-autoresearch_loop-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-autoresearch_loop.png" class="img-fluid" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="react-trace" style="flex:1 1 0; min-width:320px; margin:0;"> <div class="trace-head"><strong>Autoresearch agent logs</strong>: the target this time is <strong>MBPB</strong> (OpenADMET; mouse brain protein binding, % unbound, a distribution endpoint). We fix the label budget at <strong>n=25</strong>. Candidate sources: the other OpenADMET assays.</div> <ol> <li><span class="act">inspect_data</span> · <span class="act">source_stats</span>, i.e. coverage, value range, no-transfer floor, correlations to every candidate.</li> <li><span class="th">"MGMB correlates 0.90 with MBPB and shares 216 molecules — a valuable source."</span> → <span class="act">evaluate {MGMB}</span> <span class="obs">→ val ρ 0.51</span></li> <li><span class="th">"Add the next-best, MPPB (corr 0.61)."</span> → <span class="act">evaluate {MGMB, MPPB}</span> <span class="obs">→ val ρ 0.74</span></li> <li><span class="th">"Are they complementary or redundant?"</span> → <span class="act">run_python: train['MGMB']['smiles'].corr(...)</span> <span class="err">→ KeyError</span></li> <li><span class="th">"That key doesn't exist; use the label columns directly."</span> → <span class="act">run_python: train['MGMB'].corr(train['MPPB'])</span> <span class="obs">→ 0.46 — complementary, not redundant.</span></li> <li><span class="th">"Add LogD (corr 0.51, 961 shared)."</span> → <span class="act">evaluate {MGMB, MPPB, LogD}</span> <span class="obs">→ val ρ 0.79</span></li> <li><span class="th">"Now tune the acquisition method."</span> → sweep acquisition: <span class="act">random</span> <span class="obs">(lower)</span>, <span class="act">farthest_first at 50 labels</span> <span class="obs">(drops — "overfitting", it concludes)</span>, <span class="act">kmedoids at 25</span> <span class="obs">→ val ρ 0.81, the best yet.</span></li> <li><span class="act">finalize</span>: commit {MGMB, MPPB, LogD}, kmedoids, 25 labels, with a written rationale. <span class="obs">Held-out test ρ 0.67, against a no-transfer floor of 0.49 — but the greedy script reaches 0.72.</span></li> </ol> <div class="cap">The agent debugs its own KeyError unprompted, then talks itself into k-medoids on a 0.017 validation gain. This decision ends up losing to the greedy baseline's plain random sampling.</div> </div> </div> </div> <p>In this section, the baseline we compare to is a manual script written to perform the selection criteria sequentially: it <strong>ranks</strong> the pretraining assays by correlation to the target assay after dropping the ones with low co-occurrence or high std reads, <strong>greedily adds</strong> pretraining assays one at a time as long as the validation score is improving, and <strong>selects randomly</strong> $n$ molecules to acquire. We only had time to run this full loop on the three sparsest OpenADMET endpoints, though we would expect it to hold on TDC too, since the acquisition experiment above already located the agent’s edge in source selection rather than in molecule choice. In all assays, the agent favors complex acquisition algorithms like k-medoids or farthest-first over simple random sampling, which actually hurts its performance compared to the greedy baseline. Overall this shows that automation is possible, but it does not provide any performance advantage compared to simpler methods due to the agent’s tendency towards over-engineering.</p> <figure class="expdisc-figure"> <div class="expdisc-chart" id="expdisc-fig-autoresearch" style="width:100%; height:460px;"></div> <noscript> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/expdisc-autoresearch_vs_oracle-480.webp 480w,/assets/img/expdisc-autoresearch_vs_oracle-800.webp 800w,/assets/img/expdisc-autoresearch_vs_oracle-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/expdisc-autoresearch_vs_oracle.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </noscript> <figcaption class="caption">Full automation reaches 96% of the brute-force oracle and at best gets close to the hand-written greedy script: the agent's self-chosen k-medoids / farthest-first acquisition actually costs it against greedy's plain random sampling.</figcaption> </figure> <h2 id="conclusion">Conclusion</h2> <p>In this project, we explore the use of AI agents in transfer learning, and later in acquisition and the full property-prediction pipeline through autoresearch. The agent does help where the simple heuristics are unreliable. When assays are measured on separate molecules and there is no trustworthy correlation to rank by, the agent’s use of dataset metadata is a real edge. But when a baseline is strong it essentially ties with it, and it can underperform by reaching for overly complex methods (e.g. never choosing random acquisition in the full automation run). Given tools and no clear winner among them, it also tends to reach for what seem to be the strongest tools, sometimes at the expense of performance. In future work, scaling this evaluation up to harder baselines and tasks would further probe the usefulness of agentic frameworks in drug discovery.</p> <p>Try the agent <a href="https://admet-demo-diffalign.2.rahtiapp.fi/">here</a>: it suggests which molecules to measure next and explains each pick. The <a href="https://github.com/NajwaLaabid/assay-transfer-agent">code</a> is on GitHub.</p> <p><em>Built with Halidu Abdulai (Åbo Akademi University), Anirudh Jain (Orion), Ilari Tulkki (CSC), and Gerardo Gonzalez (Silo AI) at the Exponential Discovery in Life Sciences hackathon.</em></p> <style>.react-trace{border:1px solid rgba(128,128,128,.35);border-radius:10px;padding:1.1em 1.2em;margin:1.6em 0;font-size:.9rem;line-height:1.5;background:rgba(128,128,128,.04)}.react-trace ol{margin:0;padding-left:1.4em}.react-trace li{margin:.55em 0;padding-left:.2em}.react-trace .act{font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;font-size:.83rem;color:#7c3aed;font-weight:600}.react-trace .obs{font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;font-size:.82rem;opacity:.85}.react-trace .err{color:#c0392b}.react-trace .th{opacity:.9}.react-trace .cap{margin-top:.9em;font-size:.8rem;opacity:.7}.react-trace .trace-head{font-size:.82rem;opacity:.75;line-height:1.45;margin:0 0 .9em;padding-bottom:.7em;border-bottom:1px solid rgba(128,128,128,.3)}.twoup-cap{font-size:.82rem;opacity:.72;line-height:1.45;margin:.4em auto 0;max-width:54em;text-align:center}.loop-imgcard{flex:1 1 0;min-width:300px;border:1px solid rgba(128,128,128,.35);border-radius:10px;background:rgba(128,128,128,.04);padding:1.1em 1.2em;display:flex;align-items:center;justify-content:center}.loop-imgcard img{width:100%;height:auto}@media(prefers-color-scheme:dark){.react-trace .act{color:#a78bfa}.react-trace .err{color:#ff7a6b}}</style> <script src="/assets/js/expdisc-charts.js?0a59c484261eb3fd81e3748a5b8943af"></script>]]></content><author><name>Najwa Laabid</name></author><category term="research"/><category term="agents"/><category term="transfer-learning"/><category term="drug-discovery"/><summary type="html"><![CDATA[The purpose of this project is to test whether an agentic loop can make a meaningful difference in the property prediction stage of drug discovery. We test how well an agent can choose cheap properties to pretrain on in order to make predicting expensive ones more label-efficient. We then ask whether the same agentic design extends to data acquisition (letting the agent choose which specific molecules to measure) and to full auto-research automation (combining property selection and molecule acquisition). Our main observation is that the agent’s reasoning pays off when simpler heuristics are not reliable (e.g. too few samples to compute correlation). When a good heuristic exists, the agent ties with it at best, and can even lose by reaching for needlessly complex tools.]]></summary></entry></feed>